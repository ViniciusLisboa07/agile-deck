<div class="min-h-screen flex items-center justify-center bg-gray-100 p-4">
  <div class="w-full max-w-md bg-white shadow-md rounded p-6">
    <h1 class="text-2xl font-bold text-gray-800 mb-4"><%= @room.name %></h1>
    <p class="text-sm text-gray-600">Código da sala: <%= @room.id %></p>
    <p class="mt-2 text-gray-700">Compartilhe o link com outros usuários para eles acessarem a sala.</p>

    <div class="mt-4">
      <%= link_to 'Voltar', root_path, class: "text-blue-500 hover:text-blue-700 font-medium" %>
    </div>
  </div>
</div>
